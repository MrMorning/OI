#include <cstdio>

int main()
{
	 int n;
	 for(int i = 0; i < n; i ++)
		scanf("%d", &a[0][i]);
	 for(int i = 0; i < n; i ++)
		scanf("%d", &a[1][i]);
	 std::sort(a[0], a[0] + n), std::sort(a[1], a[1] + n);
}
