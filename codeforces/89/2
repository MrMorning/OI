#include <cstdio>
#include <cstring>
int main(){
	freopen("t.in", "r", stdin);
	scanf("%d%d", &N, &M);
	for(int i = 0; i < M; i ++){
		int u, v;
		scanf("%d%d", &u, &v);
		u --, v --;
		addEdge(u, v);
	}
	for(int i = 0; i < N; i ++){
	}
}
