#include <cstdio>

char randchar()
{
	int t = rand();
}

int main()
{
	scanf("%d%d%d", &n, &m, &seed);
	srand(seed);
	printf("%d %d\n", n, m);
	int n_pipe = rand() % n;
	for(int i = 0; i < n_pipe; i ++)
	{
		
	}
}
